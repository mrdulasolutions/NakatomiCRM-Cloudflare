# Local dev secrets — copy to .dev.vars (gitignored) and fill in.
# For staging/production use `wrangler secret put NAME --env <env>`.

JWT_SECRET=replace-with-openssl-rand-hex-32
ADMIN_BOOTSTRAP_TOKEN=optional-first-run-token
